"""Shared test fixtures for TTB Label Verification backend tests.

Real fixtures (test client, mock services, sample data) are added
in later phases as the corresponding application code is built.
"""
